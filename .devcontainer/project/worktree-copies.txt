# Paths under the main worktree to copy (not symlink) into new worktrees.
# Format: see .devcontainer/lib/list-file.sh. Patterns are interpreted as
# shell globs (`*`, `?`, `[...]`) by setup-worktree; shell substitutions
# (`$(...)`, backticks, `~`, `$VAR`) are not.
.envrcs/.envrc.secrets
.envrcs/.envrc.user
.envrcs/.env.*
.devcontainer/project/project.env
