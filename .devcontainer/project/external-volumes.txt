# Volume names (basenames) declared `external: true` in compose.override.yml.
# dev/devcontainer pre-creates each as `${DEV_PROJECT_NAME}-<basename>` before
# `dc up`, so a fresh checkout doesn't error on missing external volumes.
# Format: see .devcontainer/lib/list-file.sh.
#
# Volumes mounted at a workspace path that sibling worktrees must also see
# should additionally appear in worktree-symlinks.txt (e.g. ci/ibis-testing-data).
uv-cache
ibis-testing-data
