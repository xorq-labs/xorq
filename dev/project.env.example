# Copy to dev/project.env and uncomment to override defaults.
# PROJECT_NAME defaults to the basename of the main checkout.
# PROJECT_NAME=xorq
# MODEL_VERSION pins the Claude model used inside the container.
# MODEL_VERSION=claude-opus-4-7
